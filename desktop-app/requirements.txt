# Core dependencies from your deepfake_monitor.py script
numpy>=1.21.0
pyaudio>=0.2.11
Pillow>=9.0.0  # For PIL import
pystray>=0.19.4
requests>=2.25.0

# Auto-installed packages from your script
plyer>=2.1.0

# Build tool
pyinstaller>=5.13.0

# Note: These are built-in Python modules (no pip install needed):
# - sys, os, time, threading, queue, json, wave, io, pathlib
# - datetime, tkinter, logging, traceback, tempfile
# - winsound (Windows built-in)